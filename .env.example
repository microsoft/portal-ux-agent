# Example environment configuration for portal-ux-agent (DO NOT put secrets you can't share)
# Copy this file to .env and fill in the values.

# === Azure OpenAI (API key auth) ===
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# If using AAD instead of API key, comment the key above and uncomment these:
# AZURE_OPENAI_USE_AAD=1
# AZURE_OPENAI_SCOPE=https://cognitiveservices.azure.com/.default

# Optional logging flags
INTENT_LOG_PROMPT=0

# Static UI seeding (load src/data/default_ui/components.json at startup)
SEED_STATIC_UI=1
DEFAULT_UI_TEMPLATE=dashboard-cards-grid

# Optional: disable sample seeding even if env configured
# SEED_SAMPLE=0
