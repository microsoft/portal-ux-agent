# Dataset Changelog

## 2025-10-10 (Init)
- Added initial 5 seed tasks spanning: dashboard, kanban, admin_portal, detail_page, auth.
- Established JSON Schema `schema.json`.
- Conventions: use `required_props` not `expected_props`.

## Pending
- Expand to 50 tasks (add data_table archetype entries, harder dashboard variants with chart + KPI interplay).
- Introduce chart semantics (legend + axes expectations) for 3–5 tasks.
